<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Error state -->
<!--    <item android:color="@color/red" android:state_error="true"/>-->
    <!-- Focused state -->
    <item android:color="@color/blue" android:state_focused="true"/>
    <!-- Activated state (when text is present) -->
    <item android:color="@color/blue" android:state_activated="true"/>
    <!-- Default/unfocused state -->
    <item android:color="@color/gray600"/>
</selector>